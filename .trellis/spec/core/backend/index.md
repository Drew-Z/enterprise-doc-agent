# Core Backend Context

Use the factual project guides in `.trellis/spec/backend/`. Core owns reusable
settings, request context, health, database, logging, and telemetry contracts under
`packages/core`; it does not own application entry points.
