# API Backend Context

Use the factual project guides in `.trellis/spec/backend/`, especially directory
structure, error handling, logging, and quality. API-owned code is under `apps/api`;
shared infrastructure remains in `packages/core`.
