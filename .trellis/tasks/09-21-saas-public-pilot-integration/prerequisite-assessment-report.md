# 前置条件与中文响应候选结果

本阶段交付候选代码、回归测试、有限真实模型记录和未通过项，**保持草案，不合并、不部署**。基线 main 为 `8c62d02017e02c9c4da3dc69801572019a7f7725`；线上仍是 v0.1.44，2026-09-23 12:46 UTC 只读核查五个 Deployment 均为 1/1，应用镜像与上一版完全一致。

## 改动

`presales.v4` 要求模型列出有引用的启用前提及 met/unmet/unknown 状态。服务端拒绝未满足/未知条件搭配 supported，conditional 必须保留列出的未满足条件；合并结论及前提处明确选中的引用，保留请求内编号、逐字原文和12条上限。正文及条件/补证项检测全文非中文，但不翻译英文引文，也不限制历史草稿和人工复核。没有新服务、数据库迁移、认证/路由变更或自动重试。

## 验证

- HTTP 协议/适配器与真实 PostgreSQL 生成、复核、导出、授权检查共75项通过；生成收集器边界测试1项通过。
- 最终本地全仓非集成：1630 passed，358 deselected，23 subtests passed；包含历史未提交测试，不能当作远端提交测试数量。最终日志 `prerequisite-assessment-pytest-final.txt`。
- Ruff 全仓检查/格式、Mypy 233个源码文件、Web lint/typecheck通过。
- 响应浏览器3项与真实上传浏览器3项通过，覆盖批量/文件夹上传、失败恢复、TXT/PDF/DOCX、引用、复核、导出和租户边界。测试服务退出，新增测试租户、用户、对象及 Redis 项清理回执为0。
- 12次真实调用、无重试，使用同一 grok-4.6 路由、120秒上限；仅生成阶段，输入完整合成证据，不创建或读取公网企业。

## 原始运行与解码重放

初始协议接受10/12。H1-R5、H2-R3在前提内选中了正确来源，却因顶层没有重复引用被拒绝。这是适配器冗余约束，初始源码检查点已集中保留；修正为合法位置的明确引用集合，提示词及 schema 哈希始终为 `7cf89f233c9115c723c0bceea42a1ee085aaa4206410f34c1717d7bb9bb1981e`。两个 collector 都在修正前导入初始模块，原始运行文件不混用版本、不覆盖。

离线重放不发送模型请求、不改写原始分类。结果12/12可解码、12/12中文正文、23处引用逐字有效、21/21必要证据覆盖；H1分类6/6，H2分类4/6，总10/12，错误肯定0。输入41560、输出48134、总89694 token；计价刻度单位和账单未核实，金额未知。

H1为已知回归，H2输入及gold在首次请求前冻结。助手自审不是独立专家盲评，完整证据生成也不是公网检索/持久化验收。原始10/12与重放12/12不可混称真实首次成功率。所有原文、原始响应、重放结果及逐项审阅都存于 `evaluation/presales_quality_*v4*`，H2冻结输入为 `presales_quality_holdout_v2.json`。

## 尚未通过与下一项

H2-R5把“未提供SOC 2报告”当作不满足；H2-R6看到了互不优先的服务承诺/停机条款两侧，却仍判不满足而非资料冲突。两者都缺少相应澄清问题。H2-R2的条件以“已购买/已完成”表述，也需要改为明确待办。下一项应针对证据不足、冲突与硬限制的判定顺序和条件措辞，不放松引用或通过改gold掩盖结果。H2之后不得作为未见样本报告。

## 恢复与工作区

复用 `D:\Agent\codex\backups\tasks\20260921T060311.970Z-saas-public-pilot-integration` 中的 `presales-prerequisite-assessment` 阶段。已跟踪文件以基线提交恢复；既有未跟踪task.json使用校验过的实体恢复点，初始候选解码器另有注明原因的检查点。最终恢复依据、精确提交清单、PR与CI结果在同阶段manifest和publication回执记录。

仅在规范仓库创建候选分支，不新建clone或worktree。历史WIP、未跟踪材料、worktree、依赖与缓存均保留；本轮原始评测、测试日志与截图是交付证据，继续保留。未触发线上部署、邮件、客户资料变更或额度操作，父任务继续 in_progress。
