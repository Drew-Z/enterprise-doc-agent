{
  "ok": true,
  "url": "https://developers.cloudflare.com/workers/static-assets/direct-upload/",
  "provider": "tavily",
  "content": "[Skip to content](#main-content)\n> Documentation Index  \n> Fetch the complete documentation index at: https://developers.cloudflare.com/workers/llms.txt  \n> Use this file to discover all available pages before exploring further.\n\n[![](/_astro/logo.te5VL_aD.svg)Docs](/)\n\n[Log inDashboard](https://dash.cloudflare.com/)\n\n# Direct Uploads\n\nLast updated ||[View as Markdown](index.md)|[Agent setup](/agent-setup/)\n\nOur API empowers users to upload and include static assets as part of a Worker. These static assets can be served for free, and additionally, users can also fetch assets through an optional [assets binding](/workers/static-assets/binding/) to power more advanced applications. This guide will describe the process for attaching assets to your Worker directly with the API.\n\n```\nsequenceDiagram participant User participant Workers API User<<->>Workers API: Submit manifest  \nPOST /client/v4/accounts/:accountId/workers/scripts/:scriptName/assets-upload-session User<<->>Workers API: Upload files  \nPOST /client/v4/accounts/:accountId/workers/assets/upload?base64=true User<<->>Workers API: Upload script version  \nPUT /client/v4/accounts/:accountId/workers/scripts/:scriptName \n```\n\n```\nsequenceDiagram participant User participant Workers API User<<->>Workers API: Submit manifest  \nPOST /client/v4/accounts/:accountId/workers/dispatch/namespaces/:dispatchNamespace/scripts/:scriptName/assets-upload-session User<<->>Workers API: Upload files  \nPOST /client/v4/accounts/:accountId/workers/assets/upload?base64=true User<<->>Workers API: Upload script version  \nPUT /client/v4/accounts/:accountId/workers/dispatch/namespaces/:dispatchNamespace/scripts/:scriptName \n```\n\nThe asset upload flow can be distilled into three distinct phases:\n\n1. Registration of a manifest\n2. Upload of the assets\n3. Deployment of the Worker\n\n## Upload manifest\n\nThe asset manifest is a ledger which keeps track of files we want to use in our Worker. This manifest is used to track assets associated with each Worker version, and eliminate the need to upload unchanged files prior to a new upload.\n\nThe [manifest upload request](/api/resources/workers/subresources/scripts/subresources/assets/subresources/upload/methods/create/) describes each file which we intend to upload. Each file is its own key representing the file path and name, and is an object which contains metadata about the file.\n\n`hash` represents a 32 hexadecimal character hash of the file, while `size` is the size (in bytes) of the file.\n\n```\ncurl -X POST https://api.cloudflare.com/client/v4/accounts/{account_id}/workers/scripts/{script_name}/assets-upload-session \\ curl -X  POST https://api.cloudflare.com/client/v4/accounts/{account_id}/workers/scripts/{script_name}/assets-upload-session  \\ --header 'content-type: application/json' \\ --header 'content-type: application/json'  \\ --header 'Authorization: Bearer ' \\ --header 'Authorization: Bearer '  \\ --data '{--data '{ \"manifest\": { \"manifest\": { \"/filea.html\": { \"/filea.html\": { \"hash\": \"08f1dfda4574284ab3c21666d1\", \"hash\": \"08f1dfda4574284ab3c21666d1\", \"size\": 12 \"size\": 12 }, }, \"/fileb.html\": { \"/fileb.html\": { \"hash\": \"4f1c1af44620d531446ceef93f\", \"hash\": \"4f1c1af44620d531446ceef93f\", \"size\": 23 \"size\": 23 }, }, \"/filec.html\": { \"/filec.html\": { \"hash\": \"54995e302614e0523757a04ec1\", \"hash\": \"54995e302614e0523757a04ec1\", \"size\": 23 \"size\": 23 } } } }}'}'\n```\n\n```\ncurl -X POST https://api.cloudflare.com/client/v4/accounts/{account_id}/workers/dispatch/namespaces/{dispatch_namespace}/scripts/{script_name}/assets-upload-session \\ curl -X  POST https://api.cloudflare.com/client/v4/accounts/{account_id}/workers/dispatch/namespaces/{dispatch_namespace}/scripts/{script_name}/assets-upload-session  \\ --header 'content-type: application/json' \\ --header 'content-type: application/json'  \\ --header 'Authorization: Bearer ' \\ --header 'Authorization: Bearer '  \\ --data '{--data '{ \"manifest\": { \"manifest\": { \"/filea.html\": { \"/filea.html\": { \"hash\": \"08f1dfda4574284ab3c21666d1\", \"hash\": \"08f1dfda4574284ab3c21666d1\", \"size\": 12 \"size\": 12 }, }, \"/fileb.html\": { \"/fileb.html\": { \"hash\": \"4f1c1af44620d531446ceef93f\", \"hash\": \"4f1c1af44620d531446ceef93f\", \"size\": 23 \"size\": 23 }, }, \"/filec.html\": { \"/filec.html\": { \"hash\": \"54995e302614e0523757a04ec1\", \"hash\": \"54995e302614e0523757a04ec1\", \"size\": 23 \"size\": 23 } } } }}'}'\n```\n\nThe resulting response will contain a JWT, which provides authentication during file upload. The JWT is valid for one hour.\n\nIn addition to the JWT, the response instructs users how to optimally batch upload their files. These instructions are encoded in the `buckets` field. Each array in `buckets` contains a list of file hashes which should be uploaded together. Unmodified files will not be returned in the `buckets` field (as they do not need to be re-uploaded) if they have recently been uploaded in previous versions of your Worker.\n\n```\n{{ \"result\": { \"result\": { \"jwt\": \"\",  \"jwt\": \"\", \"buckets\": [ \"buckets\": [ [\"08f1dfda4574284ab3c21666d1\", \"4f1c1af44620d531446ceef93f\"], [\"08f1dfda4574284ab3c21666d1\", \"4f1c1af44620d531446ceef93f\"], [\"54995e302614e0523757a04ec1\"] [\"54995e302614e0523757a04ec1\"] ] ] }, }, \"success\": true,  \"success\": true, \"errors\": null,  \"errors\": null, \"messages\": null  \"messages\": null}}\n```\n\n### Limitations\n\n* Limits differ based on account plan. Refer to [Account Plan Limits](/workers/platform/limits/#account-plan-limits) for more information on limitations of static assets.\n\n## Upload Static Assets\n\nThe [file upload API](/api/resources/workers/subresources/assets/subresources/upload/methods/create/) requires files be uploaded using `multipart/form-data`. The contents of each file must be base64 encoded, and the `base64` query parameter in the URL must be set to `true`.\n\nThe provided `Content-Type` header of each file part will be attached when eventually serving the file. If you wish to avoid sending a `Content-Type` header in your deployment, `application/null` may be sent at upload time.\n\nThe `Authorization` header must be provided as a bearer token, using the JWT (upload token) from the aforementioned manifest upload call.\n\nOnce every file in the manifest has been uploaded, a status code of 201 will be returned, with the `jwt` field present. This JWT is a final \"completion\" token which can be used to create a deployment of a Worker with this set of assets. This completion token is valid for 1 hour.\n\n## Create/Deploy New Version\n\n[Script](/api/resources/workers/subresources/scripts/methods/update/), [Version](/api/resources/workers/subresources/scripts/subresources/versions/methods/create/), and [Workers for Platform script](/api/resources/workers_for_platforms/subresources/dispatch/subresources/namespaces/subresources/scripts/methods/update/) upload endpoints require specifying a metadata part in the form data. Here, we can provide the completion token from the previous (upload assets) step.\n\nExample Worker Metadata Specifying Completion Tokenbash\n\n```\n{{ \"main_module\": \"main.js\",  \"main_module\": \"main.js\", \"assets\": { \"assets\": { \"jwt\": \"\"  \"jwt\":  \"\" }, }, \"compatibility_date\": \"2021-09-14\"  \"compatibility_date\": \"2021-09-14\"}}\n```\n\nIf this is a Worker which already has assets, and you wish to just re-use the existing set of assets, we do not have to specify the completion token again. Instead, we can pass the boolean `keep_assets` option.\n\nExample Worker Metadata Specifying keep\\_assetsbash\n\n```\n{{ \"main_module\": \"main.js\",  \"main_module\": \"main.js\", \"keep_assets\": true,  \"keep_assets\":  true, \"compatibility_date\": \"2021-09-14\"  \"compatibility_date\": \"2021-09-14\"}}\n```\n\nAsset [routing configuration](/workers/wrangler/configuration/#assets) can be provided in the `assets` object, such as `html_handling` and `not_found_handling`.\n\nExample Worker Metadata Specifying Asset Configurationbash\n\n```\n{{ \"main_module\": \"main.js\",  \"main_module\": \"main.js\", \"assets\": { \"assets\": { \"jwt\": \"\",  \"jwt\": \"\", \"config\" { \"config\" { \"html_handling\": \"auto-trailing-slash\"  \"html_handling\": \"auto-trailing-slash\" } } }, }, \"compatibility_date\": \"2021-09-14\"  \"compatibility_date\": \"2021-09-14\"}}\n```\n\nOptionally, an assets binding can be provided if you wish to fetch and serve assets from within your Worker code.\n\nExample Worker Metadata Specifying Asset Bindingbash\n\n```\n{{ \"main_module\": \"main.js\",  \"main_module\": \"main.js\", \"assets\": { \"assets\": { ... ... }, }, \"bindings\": [ \"bindings\": [ ... ... { { \"name\": \"ASSETS\",  \"name\": \"ASSETS\", \"type\": \"assets\"  \"type\":  \"assets\" } } ... ... ] ] \"compatibility_date\": \"2021-09-14\"  \"compatibility_date\": \"2021-09-14\"}}\n```\n\n## Programmatic Example\n\nThis example is from [cloudflare-typescript ↗](https://github.com/cloudflare/cloudflare-typescript/blob/main/examples/workers/script-with-assets-upload.ts).\n\n```\n#!/usr/bin/env -S npm run tsn -T#!/usr/bin/env -S npm run tsn -T /**/** * Create a Worker that serves static assets * Create a Worker that serves static assets * * * This example demonstrates how to: * This example demonstrates how to: * - Upload static assets to Cloudflare Workers * - Upload static assets to Cloudflare Workers * - Create and deploy a Worker that serves those assets * - Create and deploy a Worker that serves those assets * * * Docs: * Docs: * - https://developers.cloudflare.com/workers/static-assets/direct-upload * - https://developers.cloudflare.com/workers/static-assets/direct-upload * * * Prerequisites: * Prerequisites: * 1. Generate an API token: https://developers.cloudflare.com/fundamentals/api/get-started/create-token/ * 1. Generate an API token: https://developers.cloudflare.com/fundamentals/api/get-started/create-token/ * 2. Find your account ID: https://developers.cloudflare.com/fundamentals/setup/find-account-and-zone-ids/ * 2. Find your account ID: https://developers.cloudflare.com/fundamentals/setup/find-account-and-zone-ids/ * 3. Find your workers.dev subdomain: https://developers.cloudflare.com/workers/configuration/routing/workers-dev/ * 3. Find your workers.dev subdomain: https://developers.cloudflare.com/workers/configuration/routing/workers-dev/ * * * Environment variables: * Environment variables: * - CLOUDFLARE_API_TOKEN (required) * - CLOUDFLARE_API_TOKEN (required) * - CLOUDFLARE_ACCOUNT_ID (required) * - CLOUDFLARE_ACCOUNT_ID (required) * - ASSETS_DIRECTORY (required) * - ASSETS_DIRECTORY (required) * - CLOUDFLARE_SUBDOMAIN (optional) * - CLOUDFLARE_SUBDOMAIN (optional) * * * Usage: * Usage: * Place your static files in the ASSETS_DIRECTORY, then run this script. * Place your static files in the ASSETS_DIRECTORY, then run this script. * Assets will be available at: my-script-with-assets.$subdomain.workers.dev/$filename * Assets will be available at: my-script-with-assets.$subdomain.workers.dev/$filename */ */ import crypto from \"crypto\"; import  crypto from  \"crypto\";import fs from \"fs\"; import  fs from  \"fs\";import { readFile } from \"node:fs/promises\"; import { readFile } from \"node:fs/promises\";import { extname } from \"node:path\"; import { extname } from \"node:path\";import path from \"path\"; import  path from  \"path\";import { exit } from \"node:process\"; import { exit } from \"node:process\"; import Cloudflare from \"cloudflare\"; import  Cloudflare from  \"cloudflare\"; const WORKER_NAME = \"my-worker-with-assets\"; const  WORKER_NAME = \"my-worker-with-assets\";const SCRIPT_FILENAME = `${WORKER_NAME}.mjs`; const  SCRIPT_FILENAME = `${WORKER_NAME}.mjs`; function loadConfig() {function  loadConfig() { const apiToken = process.env[\"CLOUDFLARE_API_TOKEN\"];  const  apiToken = process.env[\"CLOUDFLARE_API_TOKEN\"]; if (!apiToken) { if (!apiToken) { throw new Error( throw  new  Error( \"Missing required environment variable: CLOUDFLARE_API_TOKEN\", \"Missing required environment variable: CLOUDFLARE_API_TOKEN\", ); ); } }  const accountId = process.env[\"CLOUDFLARE_ACCOUNT_ID\"];  const  accountId = process.env[\"CLOUDFLARE_ACCOUNT_ID\"]; if (!accountId) { if (!accountId) { throw new Error( throw  new  Error( \"Missing required environment variable: CLOUDFLARE_ACCOUNT_ID\", \"Missing required environment variable: CLOUDFLARE_ACCOUNT_ID\", ); ); } }  const assetsDirectory = process.env[\"ASSETS_DIRECTORY\"];  const  assetsDirectory = process.env[\"ASSETS_DIRECTORY\"]; if (!assetsDirectory) { if (!assetsDirectory) { throw new Error(\"Missing required environment variable: ASSETS_DIRECTORY\");  throw  new  Error(\"Missing required environment variable: ASSETS_DIRECTORY\"); } }  if (!fs.existsSync(assetsDirectory)) { if (!fs. existsSync(assetsDirectory)) { throw new Error(`Assets directory does not exist: ${assetsDirectory}`);  throw  new  Error(`Assets directory does not exist: ${assetsDirectory}`); } }  const subdomain = process.env[\"CLOUDFLARE_SUBDOMAIN\"];  const  subdomain = process.env[\"CLOUDFLARE_SUBDOMAIN\"];  return { return { apiToken, apiToken, accountId, accountId, assetsDirectory, assetsDirectory, subdomain: subdomain || undefined, subdomain: subdomain ||  undefined, workerName: WORKER_NAME, workerName: WORKER_NAME, }; };}} const config = loadConfig(); const  config =  loadConfig();const client = new Cloudflare({const  client =  new  Cloudflare({ apiToken: config.apiToken, apiToken: config.apiToken,});}); /**/** * Recursively reads all files from a directory and creates a manifest * Recursively reads all files from a directory and creates a manifest * mapping file paths to their hash and size. * mapping file paths to their hash and size. */ */function createManifest(directory) {function  createManifest(directory) { const manifest = {};  const  manifest = {};  function processDirectory(currentDir, basePath = \"\") { function  processDirectory(currentDir, basePath =  \"\") { try { try { const entries = fs.readdirSync(currentDir, { withFileTypes: true });  const  entries = fs. readdirSync(currentDir, { withFileTypes: true });  for (const entry of entries) { for (const  entry  of entries) { const fullPath = path.join(currentDir, entry.name);  const  fullPath = path. join(currentDir, entry.name); const relativePath = path.join(basePath, entry.name);  const  relativePath = path. join(basePath, entry.name);  if (entry.isDirectory()) { if (entry. isDirectory()) { processDirectory(fullPath, relativePath);  processDirectory(fullPath, relativePath); } else if (entry.isFile()) { } else  if (entry. isFile()) { try { try { const fileContent = fs.readFileSync(fullPath);  const  fileContent = fs. readFileSync(fullPath); const extension = extname(relativePath).substring(1);  const  extension =  extname(relativePath). substring(1);  // Generate a hash for the file // Generate a hash for the file const hash = crypto  const  hash =  crypto .createHash(\"sha256\") . createHash(\"sha256\") .update(fileContent.toString(\"base64\") + extension) . update(fileContent. toString(\"base64\") + extension) .digest(\"hex\") . digest(\"hex\") .slice(0, 32); . slice(0, 32);  // Normalize path separators to forward slashes // Normalize path separators to forward slashes const manifestPath = `/${relativePath.replace(/\\\\/g, \"/\")}`;  const  manifestPath = `/${relativePath. replace(/ \\\\/ g, \"/\")}`;  manifest[manifestPath] = { manifest[manifestPath] = { hash, hash, size: fileContent.length, size: fileContent. length, }; };  console.log( console. log( `Added to manifest: ${manifestPath} (${fileContent.length} bytes)`, `Added to manifest: ${manifestPath} (${fileContent. length} bytes)`, ); ); } catch (error) { } catch (error) { console.warn(`Failed to process file ${fullPath}:`, error); console. warn(`Failed to process file ${fullPath}:`, error); } } } } } } } catch (error) { } catch (error) { throw new Error(`Failed to read directory ${currentDir}: ${error}`);  throw  new  Error(`Failed to read directory ${currentDir}: ${error}`); } } } }  processDirectory(directory);  processDirectory(directory);  if (Object.keys(manifest).length === 0) { if (Object. keys(manifest). length ===  0) { throw new Error(`No files found in assets directory: ${directory}`);  throw  new  Error(`No files found in assets directory: ${directory}`); } }  console.log(`Created manifest with ${Object.keys(manifest).length} files`); console. log(`Created manifest with ${Object. keys(manifest). length} files`); return manifest;  return manifest;}} /**/** * Generates the Worker script content that serves static assets * Generates the Worker script content that serves static assets */ */function generateWorkerScript(exampleFile) {function  generateWorkerScript(exampleFile) { return `  return  `export default {export default { async fetch(request, env, ctx) { async fetch(request, env, ctx) { const url = new URL(request.url); const url = new URL(request.url);  // Serve a simple index page at the root // Serve a simple index page at the root if (url.pathname === '/') { if (url.pathname === '/') { return new Response( return new Response( \\`  \\`    Static Assets Worker  Static Assets Worker      This Worker serves static assets! This Worker serves static assets!   To access your assets, add /filename to the URL.  Try visiting /${exampleFile}     To access your assets, add /filename to the URL.  Try visiting /${exampleFile}   To access your assets, add /filename to the URL. To access your assets, add /filename to the URL. Try visiting /${exampleFile} Try visiting /${exampleFile}    \\`, \\`, { { status: 200, status: 200, headers: { 'Content-Type': 'text/html' } headers: { 'Content-Type': 'text/html' } } } ); ); } }  // Serve static assets for all other paths // Serve static assets for all other paths return env.ASSETS.fetch(request); return env.ASSETS.fetch(request); } }};}; `.trim();  `. trim();}} /**/** * Creates upload payloads from buckets and manifest * Creates upload payloads from buckets and manifest */ */async function createUploadPayloads(buckets, manifest, assetsDirectory) {async  function  createUploadPayloads(buckets, manifest, assetsDirectory) { const payloads = [];  const  payloads = [];  for (const bucket of buckets) { for (const  bucket  of buckets) { const payload = {};  const  payload = {};  for (const hash of bucket) { for (const  hash  of bucket) { // Find the file path for this hash // Find the file path for this hash const manifestEntry = Object.entries(manifest).find( const  manifestEntry = Object. entries(manifest). find( ([_, data]) => data.hash === hash, ([_, data]) => data.hash === hash, ); );  if (!manifestEntry) { if (!manifestEntry) { throw new Error(`Could not find file for hash: ${hash}`);  throw  new  Error(`Could not find file for hash: ${hash}`); } }  const [relativePath] = manifestEntry;  const [relativePath] = manifestEntry; const fullPath = path.join(assetsDirectory, relativePath);  const  fullPath = path. join(assetsDirectory, relativePath);  try { try { const fileContent = await readFile(fullPath);  const  fileContent =  await  readFile(fullPath); payload[hash] = fileContent.toString(\"base64\"); payload[hash] = fileContent. toString(\"base64\"); console.log(`Prepared for upload: ${relativePath}`); console. log(`Prepared for upload: ${relativePath}`); } catch (error) { } catch (error) { throw new Error(`Failed to read file ${fullPath}: ${error}`);  throw  new  Error(`Failed to read file ${fullPath}: ${error}`); } } } }  payloads.push(payload); payloads. push(payload); } }  return payloads;  return payloads;}} /**/** * Uploads asset payloads * Uploads asset payloads */ */async function uploadAssets(payloads, uploadJwt, accountId) {async  function  uploadAssets(payloads, uploadJwt, accountId) { let completionJwt;  let completionJwt;  console.log(`Uploading ${payloads.length} payload(s)...`); console. log(`Uploading ${payloads. length} payload(s)...`);  for (let i = 0; i < payloads.length; i++) { for (let  i =  0; i < payloads. length; i ++) { const payload = payloads[i];  const  payload = payloads[i]; console.log(`Uploading payload ${i + 1}/${payloads.length}...`); console. log(`Uploading payload ${i +  1}/${payloads. length}...`);  try { try { const response = await client.workers.assets.upload.create( const  response =  await client.workers.assets.upload. create( { { account_id: accountId, account_id: accountId, base64: true, base64: true, body: payload, body: payload, }, }, { { headers: { Authorization: `Bearer ${uploadJwt}` }, headers: { Authorization: `Bearer ${uploadJwt}` }, }, }, ); );  if (response?.jwt) { if (response?.jwt) { completionJwt = response.jwt;  completionJwt = response.jwt; } } } catch (error) { } catch (error) { throw new Error(`Failed to upload payload ${i + 1}: ${error}`);  throw  new  Error(`Failed to upload payload ${i +  1}: ${error}`); } } } }  if (!completionJwt) { if (!completionJwt) { throw new Error(\"Upload completed but no completion JWT received\");  throw  new  Error(\"Upload completed but no completion JWT received\"); } }  console.log(\"✅ All assets uploaded successfully\"); console. log(\"✅ All assets uploaded successfully\"); return completionJwt;  return completionJwt;}} async function main() {async  function  main() { try { try { console.log( console. log( \"🚀 Starting Worker creation and deployment with static assets...\", \"🚀 Starting Worker creation and deployment with static assets...\", ); ); console.log(`📁 Assets directory: ${config.assetsDirectory}`); console. log(`📁 Assets directory: ${config. assetsDirectory}`);  console.log(\"📝 Creating asset manifest...\"); console. log(\"📝 Creating asset manifest...\"); const manifest = createManifest(config.assetsDirectory);  const  manifest =  createManifest(config.assetsDirectory); const exampleFile =  const  exampleFile = Object.keys(manifest)[0]?.replace(/^\\//, \"\") || \"file.txt\"; Object. keys(manifest)[0]?. replace(/ ^\\//, \"\") || \"file.txt\";  const scriptContent = generateWorkerScript(exampleFile);  const  scriptContent =  generateWorkerScript(exampleFile);  let worker;  let worker; try { try { worker = await client.workers.beta.workers.get(config.workerName, { worker =  await client.workers.beta.workers. get(config.workerName, { account_id: config.accountId, account_id: config.accountId, }); }); console.log(`♻️ Worker ${config.workerName} already exists. Using it.`); console. log(`♻️ Worker ${config. workerName} already exists. Using it.`); } catch (error) { } catch (error) { if (!(error instanceof Cloudflare.NotFoundError)) { if (!(error instanceof  Cloudflare. NotFoundError)) { throw error;  throw error; } } console.log(`✏️ Creating Worker ${config.workerName}...`); console. log(`✏️ Creating Worker ${config. workerName}...`); worker = await client.workers.beta.workers.create({ worker =  await client.workers.beta.workers. create({ account_id: config.accountId, account_id: config.accountId, name: config.workerName, name: config.workerName, subdomain: { subdomain: { enabled: config.subdomain !== undefined, enabled: config.subdomain !==  undefined, }, }, observability: { observability: { enabled: true, enabled: true, }, }, }); }); } }  console.log(`⚙️ Worker id: ${worker.id}`); console. log(`⚙️ Worker id: ${worker. id}`); console.log(\"🔄 Starting asset upload session...\"); console. log(\"🔄 Starting asset upload session...\");  const uploadResponse = await client.workers.scripts.assets.upload.create( const  uploadResponse =  await client.workers.scripts.assets.upload. create( config.workerName, config.workerName, { { account_id: config.accountId, account_id: config.accountId, manifest, manifest, }, }, ); );  const { buckets, jwt: uploadJwt } = uploadResponse;  const { buckets, jwt: uploadJwt } = uploadResponse;  if (!uploadJwt || !buckets) { if (! uploadJwt || !buckets) { throw new Error(\"Failed to start asset upload session\");  throw  new  Error(\"Failed to start asset upload session\"); } }  let completionJwt;  let completionJwt;  if (buckets.length === 0) { if (buckets. length ===  0) { console.log(\"✅ No new assets to upload!\"); console. log(\"✅ No new assets to upload!\"); // Use the initial upload JWT as completion JWT when no uploads are needed // Use the initial upload JWT as completion JWT when no uploads are needed completionJwt = uploadJwt;  completionJwt = uploadJwt; } else { } else { const payloads = await createUploadPayloads( const  payloads =  await  createUploadPayloads( buckets, buckets, manifest, manifest, config.assetsDirectory, config.assetsDirectory, ); );  completionJwt = await uploadAssets(payloads, uploadJwt, config.accountId);  completionJwt =  await  uploadAssets(payloads, uploadJwt, config.accountId); } }  console.log(\"✏️ Creating Worker version...\"); console. log(\"✏️ Creating Worker version...\");  // Create a new version with assets // Create a new version with assets const version = await client.workers.beta.workers.versions.create( const  version =  await client.workers.beta.workers.versions. create( worker.id, worker.id, { { account_id: config.accountId, account_id: config.accountId, main_module: SCRIPT_FILENAME, main_module: SCRIPT_FILENAME, compatibility_date: new Date().toISOString().split(\"T\")[0], compatibility_date: new  Date(). toISOString(). split(\"T\")[0], bindings: [ bindings: [ { { type: \"assets\", type: \"assets\", name: \"ASSETS\", name: \"ASSETS\", }, }, ], ], assets: { assets: { jwt: completionJwt, jwt: completionJwt, }, }, modules: [ modules: [ { { name: SCRIPT_FILENAME, name: SCRIPT_FILENAME, content_type: \"application/javascript+module\", content_type: \"application/javascript+module\", content_base64: Buffer.from(scriptContent).toString(\"base64\"), content_base64: Buffer. from(scriptContent). toString(\"base64\"), }, }, ], ], }, }, ); );  console.log(\"🚚 Creating Worker deployment...\"); console. log(\"🚚 Creating Worker deployment...\");  // Create a deployment and point all traffic to the version we created // Create a deployment and point all traffic to the version we created await client.workers.scripts.deployments.create(config.workerName, { await client.workers.scripts.deployments. create(config.workerName, { account_id: config.accountId, account_id: config.accountId, strategy: \"percentage\", strategy: \"percentage\", versions: [ versions: [ { { percentage: 100, percentage: 100, version_id: version.id, version_id: version.id, }, }, ], ], }); });  console.log(\"✅ Deployment successful!\"); console. log(\"✅ Deployment successful!\");  if (config.subdomain) { if (config.subdomain) { console.log(` console. log(`🌍 Your Worker is live!🌍 Your Worker is live!📍 Base URL: https://${config.workerName}.${config.subdomain}.workers.dev/📍 Base URL: https://${config. workerName}.${config. subdomain}.workers.dev/📄 Try accessing: https://${config.workerName}.${config.subdomain}.workers.dev/${exampleFile}📄 Try accessing: https://${config. workerName}.${config. subdomain}.workers.dev/${exampleFile}`); `); } else { } else { console.log(` console. log(`⚠️ Set up a route, custom domain, or workers.dev subdomain to access your Worker.⚠️ Set up a route, custom domain, or workers.dev subdomain to access your Worker.Add CLOUDFLARE_SUBDOMAIN to your environment variables to set one up automatically.Add CLOUDFLARE_SUBDOMAIN to your environment variables to set one up automatically.`); `); } } } catch (error) { } catch (error) { console.error(\"❌ Deployment failed:\", error); console. error(\"❌ Deployment failed:\", error); exit(1);  exit(1); } }}} main(); main();\n```\n\n```\n#!/usr/bin/env -S npm run tsn -T#!/usr/bin/env -S npm run tsn -T /**/** * Create a Worker that serves static assets * Create a Worker that serves static assets * * * This example demonstrates how to: * This example demonstrates how to: * - Upload static assets to Cloudflare Workers * - Upload static assets to Cloudflare Workers * - Create and deploy a Worker that serves those assets * - Create and deploy a Worker that serves those assets * * * Docs: * Docs: * - https://developers.cloudflare.com/workers/static-assets/direct-upload * - https://developers.cloudflare.com/workers/static-assets/direct-upload * * * Prerequisites: * Prerequisites: * 1. Generate an API token: https://developers.cloudflare.com/fundamentals/api/get-started/create-token/ * 1. Generate an API token: https://developers.cloudflare.com/fundamentals/api/get-started/create-token/ * 2. Find your account ID: https://developers.cloudflare.com/fundamentals/setup/find-account-and-zone-ids/ * 2. Find your account ID: https://developers.cloudflare.com/fundamentals/setup/find-account-and-zone-ids/ * 3. Find your workers.dev subdomain: https://developers.cloudflare.com/workers/configuration/routing/workers-dev/ * 3. Find your workers.dev subdomain: https://developers.cloudflare.com/workers/configuration/routing/workers-dev/ * * * Environment variables: * Environment variables: * - CLOUDFLARE_API_TOKEN (required) * - CLOUDFLARE_API_TOKEN (required) * - CLOUDFLARE_ACCOUNT_ID (required) * - CLOUDFLARE_ACCOUNT_ID (required) * - ASSETS_DIRECTORY (required) * - ASSETS_DIRECTORY (required) * - CLOUDFLARE_SUBDOMAIN (optional) * - CLOUDFLARE_SUBDOMAIN (optional) * * * Usage: * Usage: * Place your static files in the ASSETS_DIRECTORY, then run this script. * Place your static files in the ASSETS_DIRECTORY, then run this script. * Assets will be available at: my-script-with-assets.$subdomain.workers.dev/$filename * Assets will be available at: my-script-with-assets.$subdomain.workers.dev/$filename */ */ import crypto from 'crypto'; import  crypto from  'crypto';import fs from 'fs'; import  fs from  'fs';import { readFile } from 'node:fs/promises'; import { readFile } from 'node:fs/promises';import { extname } from 'node:path'; import { extname } from 'node:path';import path from 'path'; import  path from  'path';import { exit } from 'node:process'; import { exit } from 'node:process'; import Cloudflare from 'cloudflare'; import  Cloudflare from  'cloudflare'; interface Config {interface  Config { apiToken: string;  apiToken:  string; accountId: string;  accountId:  string; assetsDirectory: string;  assetsDirectory:  string; subdomain: string | undefined;  subdomain:  string  |  undefined; workerName: string;  workerName:  string;}} interface AssetManifest {interface  AssetManifest { [path: string]: { [path:  string]: { hash: string;  hash:  string; size: number;  size:  number; }; };}} interface UploadPayload {interface  UploadPayload { [hash: string]: string; // base64 encoded content [hash:  string]:  string; // base64 encoded content}} const WORKER_NAME = 'my-worker-with-assets'; const  WORKER_NAME = 'my-worker-with-assets';const SCRIPT_FILENAME = `${WORKER_NAME}.mjs`; const  SCRIPT_FILENAME = `${WORKER_NAME}.mjs`; function loadConfig(): Config {function  loadConfig():  Config { const apiToken = process.env['CLOUDFLARE_API_TOKEN'];  const  apiToken = process.env['CLOUDFLARE_API_TOKEN']; if (!apiToken) { if (!apiToken) { throw new Error('Missing required environment variable: CLOUDFLARE_API_TOKEN');  throw  new  Error('Missing required environment variable: CLOUDFLARE_API_TOKEN'); } }  const accountId = process.env['CLOUDFLARE_ACCOUNT_ID'];  const  accountId = process.env['CLOUDFLARE_ACCOUNT_ID']; if (!accountId) { if (!accountId) { throw new Error('Missing required environment variable: CLOUDFLARE_ACCOUNT_ID');  throw  new  Error('Missing required environment variable: CLOUDFLARE_ACCOUNT_ID'); } }  const assetsDirectory = process.env['ASSETS_DIRECTORY'];  const  assetsDirectory = process.env['ASSETS_DIRECTORY']; if (!assetsDirectory) { if (!assetsDirectory) { throw new Error('Missing required environment variable: ASSETS_DIRECTORY');  throw  new  Error('Missing required environment variable: ASSETS_DIRECTORY'); } }  if (!fs.existsSync(assetsDirectory)) { if (!fs. existsSync(assetsDirectory)) { throw new Error(`Assets directory does not exist: ${assetsDirectory}`);  throw  new  Error(`Assets directory does not exist: ${assetsDirectory}`); } }  const subdomain = process.env['CLOUDFLARE_SUBDOMAIN'];  const  subdomain = process.env['CLOUDFLARE_SUBDOMAIN'];  return { return { apiToken, apiToken, accountId, accountId, assetsDirectory, assetsDirectory, subdomain: subdomain || undefined, subdomain: subdomain ||  undefined, workerName: WORKER_NAME, workerName: WORKER_NAME, }; };}} const config = loadConfig(); const  config =  loadConfig();const client = new Cloudflare({const  client =  new  Cloudflare({ apiToken: config.apiToken, apiToken: config.apiToken,});}); /**/** * Recursively reads all files from a directory and creates a manifest * Recursively reads all files from a directory and creates a manifest * mapping file paths to their hash and size. * mapping file paths to their hash and size. */ */function createManifest(directory: string): AssetManifest {function  createManifest(directory:  string):  AssetManifest { const manifest: AssetManifest = {};  const  manifest:  AssetManifest = {};  function processDirectory(currentDir: string, basePath = ''): void { function  processDirectory(currentDir:  string, basePath =  ''):  void { try { try { const entries = fs.readdirSync(currentDir, { withFileTypes: true });  const  entries = fs. readdirSync(currentDir, { withFileTypes: true });  for (const entry of entries) { for (const  entry  of entries) { const fullPath = path.join(currentDir, entry.name);  const  fullPath = path. join(currentDir, entry.name); const relativePath = path.join(basePath, entry.name);  const  relativePath = path. join(basePath, entry.name);  if (entry.isDirectory()) { if (entry. isDirectory()) { processDirectory(fullPath, relativePath);  processDirectory(fullPath, relativePath); } else if (entry.isFile()) { } else  if (entry. isFile()) { try { try { const fileContent = fs.readFileSync(fullPath);  const  fileContent = fs. readFileSync(fullPath); const extension = extname(relativePath).substring(1);  const  extension =  extname(relativePath). substring(1);  // Generate a hash for the file // Generate a hash for the file const hash = crypto  const  hash =  crypto .createHash('sha256') . createHash('sha256') .update(fileContent.toString('base64') + extension) . update(fileContent. toString('base64') + extension) .digest('hex') . digest('hex') .slice(0, 32); . slice(0, 32);  // Normalize path separators to forward slashes // Normalize path separators to forward slashes const manifestPath = `/${relativePath.replace(/\\\\/g, '/')}`;  const  manifestPath = `/${relativePath. replace(/ \\\\/ g, '/')}`;  manifest[manifestPath] = { manifest[manifestPath] = { hash, hash, size: fileContent.length, size: fileContent. length, }; };  console.log(`Added to manifest: ${manifestPath} (${fileContent.length} bytes)`); console. log(`Added to manifest: ${manifestPath} (${fileContent. length} bytes)`); } catch (error) { } catch (error) { console.warn(`Failed to process file ${fullPath}:`, error); console. warn(`Failed to process file ${fullPath}:`, error); } } } } } } } catch (error) { } catch (error) { throw new Error(`Failed to read directory ${currentDir}: ${error}`);  throw  new  Error(`Failed to read directory ${currentDir}: ${error}`); } } } }  processDirectory(directory);  processDirectory(directory);  if (Object.keys(manifest).length === 0) { if (Object. keys(manifest). length ===  0) { throw new Error(`No files found in assets directory: ${directory}`);  throw  new  Error(`No files found in assets directory: ${directory}`); } }  console.log(`Created manifest with ${Object.keys(manifest).length} files`); console. log(`Created manifest with ${Object. keys(manifest). length} files`); return manifest;  return manifest;}} /**/** * Generates the Worker script content that serves static assets * Generates the Worker script content that serves static assets */ */function generateWorkerScript(exampleFile: string): string {function  generateWorkerScript(exampleFile:  string):  string { return `  return  `export default {export default { async fetch(request, env, ctx) { async fetch(request, env, ctx) { const url = new URL(request.url); const url = new URL(request.url);  // Serve a simple index page at the root // Serve a simple index page at the root if (url.pathname === '/') { if (url.pathname === '/') { return new Response( return new Response( \\`  \\`    Static Assets Worker  Static Assets Worker      This Worker serves static assets! This Worker serves static assets!   To access your assets, add /filename to the URL.  Try visiting /${exampleFile}     To access your assets, add /filename to the URL.  Try visiting /${exampleFile}   To access your assets, add /filename to the URL. To access your assets, add /filename to the URL. Try visiting /${exampleFile} Try visiting /${exampleFile}    \\`, \\`, { { status: 200, status: 200, headers: { 'Content-Type': 'text/html' } headers: { 'Content-Type': 'text/html' } } } ); ); } }  // Serve static assets for all other paths // Serve static assets for all other paths return env.ASSETS.fetch(request); return env.ASSETS.fetch(request); } }};}; `.trim();  `. trim();}} /**/** * Creates upload payloads from buckets and manifest * Creates upload payloads from buckets and manifest */ */async function createUploadPayloads(async  function  createUploadPayloads( buckets: string[][],  buckets:  string[][], manifest: AssetManifest,  manifest:  AssetManifest, assetsDirectory: string  assetsDirectory:  string): Promise<UploadPayload[]> {):  Promise< UploadPayload[]> { const payloads: UploadPayload[] = [];  const  payloads:  UploadPayload[] = [];  for (const bucket of buckets) { for (const  bucket  of buckets) { const payload: UploadPayload = {};  const  payload:  UploadPayload = {};  for (const hash of bucket) { for (const  hash  of bucket) { // Find the file path for this hash // Find the file path for this hash const manifestEntry = Object.entries(manifest).find( const  manifestEntry = Object. entries(manifest). find( ([_, data]) => data.hash === hash ([_, data]) => data.hash ===  hash ); );  if (!manifestEntry) { if (!manifestEntry) { throw new Error(`Could not find file for hash: ${hash}`);  throw  new  Error(`Could not find file for hash: ${hash}`); } }  const [relativePath] = manifestEntry;  const [relativePath] = manifestEntry; const fullPath = path.join(assetsDirectory, relativePath);  const  fullPath = path. join(assetsDirectory, relativePath);  try { try { const fileContent = await readFile(fullPath);  const  fileContent =  await  readFile(fullPath); payload[hash] = fileContent.toString('base64'); payload[hash] = fileContent. toString('base64'); console.log(`Prepared for upload: ${relativePath}`); console. log(`Prepared for upload: ${relativePath}`); } catch (error) { } catch (error) { throw new Error(`Failed to read file ${fullPath}: ${error}`);  throw  new  Error(`Failed to read file ${fullPath}: ${error}`); } } } }  payloads.push(payload); payloads. push(payload); } }  return payloads;  return payloads;}} /**/** * Uploads asset payloads * Uploads asset payloads */ */async function uploadAssets(async  function  uploadAssets( payloads: UploadPayload[],  payloads:  UploadPayload[], uploadJwt: string,  uploadJwt:  string, accountId: string  accountId:  string): Promise<string> {):  Promise< string> { let completionJwt: string | undefined;  let  completionJwt:  string  |  undefined;  console.log(`Uploading ${payloads.length} payload(s)...`); console. log(`Uploading ${payloads. length} payload(s)...`);  for (let i = 0; i < payloads.length; i++) { for (let  i =  0; i < payloads. length; i ++) { const payload = payloads[i]!;  const  payload = payloads[i]!; console.log(`Uploading payload ${i + 1}/${payloads.length}...`); console. log(`Uploading payload ${i +  1}/${payloads. length}...`);  try { try { const response = await client.workers.assets.upload.create( const  response =  await client.workers.assets.upload. create( { { account_id: accountId, account_id: accountId, base64: true, base64: true, body: payload, body: payload, }, }, { { headers: { Authorization: `Bearer ${uploadJwt}` }, headers: { Authorization: `Bearer ${uploadJwt}` }, } } ); );  if (response?.jwt) { if (response?.jwt) { completionJwt = response.jwt;  completionJwt = response.jwt; } } } catch (error) { } catch (error) { throw new Error(`Failed to upload payload ${i + 1}: ${error}`);  throw  new  Error(`Failed to upload payload ${i +  1}: ${error}`); } } } }  if (!completionJwt) { if (!completionJwt) { throw new Error('Upload completed but no completion JWT received');  throw  new  Error('Upload completed but no completion JWT received'); } }  console.log('✅ All assets uploaded successfully'); console. log('✅ All assets uploaded successfully'); return completionJwt;  return completionJwt;}} async function main(): Promise<void> {async  function  main():  Promise< void> { try { try { console.log('🚀 Starting Worker creation and deployment with static assets...'); console. log('🚀 Starting Worker creation and deployment with static assets...'); console.log(`📁 Assets directory: ${config.assetsDirectory}`); console. log(`📁 Assets directory: ${config. assetsDirectory}`);  console.log('📝 Creating asset manifest...'); console. log('📝 Creating asset manifest...'); const manifest = createManifest(config.assetsDirectory);  const  manifest =  createManifest(config.assetsDirectory); const exampleFile = Object.keys(manifest)[0]?.replace(/^\\//, '') || 'file.txt';  const  exampleFile = Object. keys(manifest)[0]?. replace(/ ^\\//, '') || 'file.txt';  const scriptContent = generateWorkerScript(exampleFile);  const  scriptContent =  generateWorkerScript(exampleFile);  let worker;  let worker; try { try { worker = await client.workers.beta.workers.get(config.workerName, { worker =  await client.workers.beta.workers. get(config.workerName, { account_id: config.accountId, account_id: config.accountId, }); }); console.log(`♻️ Worker ${config.workerName} already exists. Using it.`); console. log(`♻️ Worker ${config. workerName} already exists. Using it.`); } catch (error) { } catch (error) { if (!(error instanceof Cloudflare.NotFoundError)) { throw error; }  if (!(error instanceof  Cloudflare. NotFoundError)) { throw error; } console.log(`✏️ Creating Worker ${config.workerName}...`); console. log(`✏️ Creating Worker ${config. workerName}...`); worker = await client.workers.beta.workers.create({ worker =  await client.workers.beta.workers. create({ account_id: config.accountId, account_id: config.accountId, name: config.workerName, name: config.workerName, subdomain: { subdomain: { enabled: config.subdomain !== undefined, enabled: config.subdomain !==  undefined, }, }, observability: { observability: { enabled: true, enabled: true, }, }, }); }); } }  console.log(`⚙️ Worker id: ${worker.id}`); console. log(`⚙️ Worker id: ${worker. id}`); console.log('🔄 Starting asset upload session...'); console. log('🔄 Starting asset upload session...');  const uploadResponse = await client.workers.scripts.assets.upload.create( const  uploadResponse =  await client.workers.scripts.assets.upload. create( config.workerName, config.workerName, { { account_id: config.accountId, account_id: config.accountId, manifest, manifest, } } ); );  const { buckets, jwt: uploadJwt } = uploadResponse;  const { buckets, jwt: uploadJwt } = uploadResponse;  if (!uploadJwt || !buckets) { if (! uploadJwt || !buckets) { throw new Error('Failed to start asset upload session');  throw  new  Error('Failed to start asset upload session'); } }  let completionJwt: string;  let  completionJwt:  string;  if (buckets.length === 0) { if (buckets. length ===  0) { console.log('✅ No new assets to upload!'); console. log('✅ No new assets to upload!'); // Use the initial upload JWT as completion JWT when no uploads are needed // Use the initial upload JWT as completion JWT when no uploads are needed completionJwt = uploadJwt;  completionJwt = uploadJwt; } else { } else { const payloads = await createUploadPayloads( const  payloads =  await  createUploadPayloads( buckets, buckets, manifest, manifest, config.assetsDirectory config.assetsDirectory ); );  completionJwt = await uploadAssets( completionJwt =  await  uploadAssets( payloads, payloads, uploadJwt, uploadJwt, config.accountId config.accountId ); ); } }  console.log('✏️ Creating Worker version...'); console. log('✏️ Creating Worker version...');  // Create a new version with assets // Create a new version with assets const version = await client.workers.beta.workers.versions.create(worker.id, { const  version =  await client.workers.beta.workers.versions. create(worker.id, { account_id: config.accountId, account_id: config.accountId, main_module: SCRIPT_FILENAME, main_module: SCRIPT_FILENAME, compatibility_date: new Date().toISOString().split('T')[0]!, compatibility_date: new  Date(). toISOString(). split('T')[0]!, bindings: [ bindings: [ { { type: 'assets', type: 'assets', name: 'ASSETS', name: 'ASSETS', }, }, ], ], assets: { assets: { jwt: completionJwt, jwt: completionJwt, }, }, modules: [ modules: [ { { name: SCRIPT_FILENAME, name: SCRIPT_FILENAME, content_type: 'application/javascript+module', content_type: 'application/javascript+module', content_base64: Buffer.from(scriptContent).toString('base64'), content_base64: Buffer. from(scriptContent). toString('base64'), }, }, ], ], }); });  console.log('🚚 Creating Worker deployment...'); console. log('🚚 Creating Worker deployment...');  // Create a deployment and point all traffic to the version we created // Create a deployment and point all traffic to the version we created await client.workers.scripts.deployments.create(config.workerName, { await client.workers.scripts.deployments. create(config.workerName, { account_id: config.accountId, account_id: config.accountId, strategy: 'percentage', strategy: 'percentage', versions: [ versions: [ { { percentage: 100, percentage: 100, version_id: version.id, version_id: version.id, }, }, ], ], }); });  console.log('✅ Deployment successful!'); console. log('✅ Deployment successful!');  if (config.subdomain) { if (config.subdomain) { console.log(` console. log(`🌍 Your Worker is live!🌍 Your Worker is live!📍 Base URL: https://${config.workerName}.${config.subdomain}.workers.dev/📍 Base URL: https://${config. workerName}.${config. subdomain}.workers.dev/📄 Try accessing: https://${config.workerName}.${config.subdomain}.workers.dev/${exampleFile}📄 Try accessing: https://${config. workerName}.${config. subdomain}.workers.dev/${exampleFile}`); `); } else { } else { console.log(` console. log(`⚠️ Set up a route, custom domain, or workers.dev subdomain to access your Worker.⚠️ Set up a route, custom domain, or workers.dev subdomain to access your Worker.Add CLOUDFLARE_SUBDOMAIN to your environment variables to set one up automatically.Add CLOUDFLARE_SUBDOMAIN to your environment variables to set one up automatically.`); `); } } } catch (error) { } catch (error) { console.error('❌ Deployment failed:', error); console. error('❌ Deployment failed:', error); exit(1);  exit(1); } }}} main(); main();\n```\n\nWas this helpful?",
  "provider_attempts": [
    {
      "capability": "web_fetch",
      "provider": "tavily",
      "status": "ok",
      "error_type": "",
      "error": "",
      "elapsed_ms": 2185.35,
      "result_count": 1
    }
  ],
  "fallback_used": false,
  "elapsed_ms": 2185.36
}