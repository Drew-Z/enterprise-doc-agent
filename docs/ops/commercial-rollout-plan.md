# 当前 4C4G 候选发布与回滚方案

状态：2026-09-26 已完成线上只读基线、已提交基础设施模板的离线渲染及部署失败恢复修复。本文是执行准备，尚无新版本标签、可拉取候选镜像、候选迁移或线上切换；正式容量和商业准入仍待验收。

## 固定版本和操作边界

| 对象 | 来源及状态 |
| --- | --- |
| 应用候选基础 | `e8aa9e29a26692ca7336e36eaa1dbd8733db7cf8`，队列/Redis 采样已经实现；本方案所在提交另加入工作流失败恢复守卫 |
| 发布执行源码 | 应使用包含本方案且 Quality/Container Supply Chain 通过的精确提交；发布前将 40 位 SHA 写入私有执行回执，不能使用浮动分支 |
| 拟制备兼容回退候选 | `93d95a96d83b9ec7ac26897b9287842d0d798923`，已含 0031 与结构化前提读取；尚无已验证可拉取的回退镜像或镜像级回滚演练 |
| 当前环境 | `enterprise-doc-agent-staging`，profile=`single-node-4c4g`，已有主机，不新增服务器或常驻监控 |
| 当前数据库 | 2026-09-26 06:35 UTC 只读查询为 `20260923_0027` |
| 当前供应商配置 | GitHub 浏览器认证、真实向量配置 version=3；不更换模型/向量、登录提供商或用户凭据 |
| 本轮执行预算 | 真实模型/向量请求 0、发信 0、远程部署/迁移 0；只读核查和本地验证已执行 |

当前部署镜像与此前 v0.1.44 记录一致，但 Deployment 无版本标签；本轮的摘要核对不重新证明源码或签名。完整 Pod/配置摘要和读取时间只保存在集中恢复组，不提交私有原始库存。

| 组件 | 当前 Deployment revision | 当前配置镜像摘要 | 副本 | CPU limit / 内存 limit |
| --- | ---: | --- | ---: | --- |
| API | 16 | `sha256:95d6dd43752c0c56a59e40278a8af8ed3c0b4d897fa47f069d50afc4c8c86d9d` | 1 | 600m / 512Mi |
| Worker | 16 | `sha256:3fe165ee379702c3f0c18189f495693911230205a9b570ba568a8cf239ce48f1` | 1 | 700m / 640Mi |
| Consumer | 16 | `sha256:cfe0e5feb3ed852a095abb4ef74f892fffdcf110509e4d1d66b6cab5fa2d2677` | 1 | 500m / 384Mi |
| Web | 12 | `sha256:109e070fa0cbe668ce71ae2a39f2249e502d15b1ff3c4cd386c5c0828ccfbe92` | 1 | 150m / 128Mi |
| Redis | 1 | `sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99` | 1 | 200m / 192Mi |

四个应用采用 maxSurge=0 / maxUnavailable=1，Redis 为 Recreate；切换不能承诺零停机。离线 Kustomize 5.7.1 从精确提交渲染出 25 个资源，五个 Deployment 的副本/资源与现网一致。模板尚未绑定发布镜像和实际配置，不能直接 apply。当前主机物理内存 3,904,057,344 字节；一次只读低负载值不能作为容量通过证据。

## 先完成可恢复的镜像包

1. 拟在现有 `ghcr.io/drew-z` 制备两组四组件镜像：回退候选 `v0.1.45-rc.0` 指向 `93d95a9…`；部署候选 `v0.1.45-rc.1` 指向包含本方案的获准精确提交。2026-09-26 只读检查这两个拟用标签均不存在；创建前再次检查，不覆盖标签。这里只是提案，尚未创建远程标签。
2. 复用 `container.yml` 的标签流程：构建、逐摘要扫描、SBOM、BuildKit provenance、签名及验证，保存两份严格 release manifest。普通 PR 镜像仅在 CI runner 内构建扫描，不会推送到仓库；PR 绿灯不能填成已发布镜像摘要。
3. 以实际发布摘要在本机独占恢复副本验证候选→回退候选：数据库保持 0031，新旧生成稿/复核/CSV、企业隔离、幂等与账本历史都须能读回。源码核对发现两候选的 Presales、迁移、API 与 Web 业务代码未变，但这不替代镜像级演练。
4. 制备镜像包只发布构建产物，不 dispatch `deploy-staging.yml`，不合并 PR、不更改 main、不运行模型或向量请求。当前 CI 没有标签自动部署触发器。标签和镜像发布需按精确 SHA/标签单独执行获准操作。

## 数据库与配置差异

| 迁移 | 主要变化 | 发布前须处理 |
| --- | --- | --- |
| 0028 | 售前后台状态、任务绑定、供应商调用及路由健康 | 排空旧版运行请求；新状态/调用历史不能被旧版覆盖 |
| 0029 | Agent 成功任务与文档处理配额、预留/结算事件 | 审核受邀企业及演示企业的有限额度；迁移本身不会为已有企业自动填入商业额度 |
| 0030 | 独立供应商 HTTP 尝试账本 | 旧版创建且尚未完成的任务必须先处理，避免跨版本执行缺少预留/记账 |
| 0031 | 供应商请求标识与对账索引 | 保留已有历史，禁止把未知 token/费用改成零 |

额度应通过 `manage_tenant_entitlements.py configure-products` 先预览，再以明确企业、当前 entitlement/version、Agent 任务数和文档字节数执行经批准配置。不要直接更新计数、重置额度周期或以无限额度解决升级后的拒绝。真实企业额度值尚未在本方案中批准。

第一阶段配置保持 `PRESALES__BACKGROUND_GENERATION_ENABLED=false`、`PRESALES__AUTOMATIC_FAILOVER_ENABLED=false`。现网这两个键缺省，新候选默认均为 false；发布包须显式写入，不依赖环境继承。运行采样保持 `OTEL__METRICS_ENABLED=true`。开启后台及切换属于后续受控试验的单独配置差异。

现有 `deploy-staging.yml` 每次会执行 embedding rollout，认证 smoke 也会上传/调用真实服务；`run_smoke=false` **不会**跳过 embedding rollout。因此当前零供应商预算不能直接 dispatch 默认部署流程。下一执行包须先落实“保留现有向量且零供应商请求”的具体路线，或取得明确有界的调用批准；不能仅在报告中声称未调用，也不能把跳过验证记为通过。

## 获准切换后的阶段顺序

1. **开窗前核对。** 读回 namespace UID、配置摘要、运行镜像/副本、数据库版本和发布 manifest；任一项与批准清单不一致则停止。准备同一冻结写入窗口的 DB 与对象恢复点，放入当前集中恢复组；9 月 25 日历史快照不能充当本次切换前备份。
2. **暂停新业务并排空。** 采用经审阅的维护入口/停止 API、Web 提交，确认外部 MCP/其他写入方也停止。Worker/Consumer 只完成既有任务，禁止补发历史失败。核对 jobs 的 pending/retry_wait/running、Presales 的 running/queued/recovering，以及 Agent 的 pending/running/waiting_approval 和活动上传；有旧版可恢复任务时先处理，不清空队列或改写状态来凑零。然后停止 Worker/Consumer。当前只读计数不替代开窗前检查，也没有完成 Agent/上传全量排空验收。
3. **备份与迁移。** 核对完整恢复点哈希后，以候选 migration Job 升至 `20260924_0031`。保留原有迁移 Job 成败、实际版本和耗时；失败或超时立即停止自动推进。不能把 Job 的超时等同于数据库没有提交。
4. **配置与应用。** 将经审核的有限企业额度、四组件发布摘要、GitHub/对象/向量原配置与显式功能开关一起固定。使用已有 prerequisites 渲染/校验流程，保持管理员所有权，不扩大部署服务账号权限。四个应用按同一候选发布，Redis 和数据卷不重建；MCP 如由独立进程使用，也须同步到兼容版本或维持停用。
5. **保持维护，先读后写。** 验证 ready、登录入口及授权、历史文档/响应表读取、跨租户拒绝；核对实际 0031、镜像/配置与每个 Worker 的成功/时间戳。无真实供应商预算时不做真实上传解析/生成验证，也不对真实企业开启提交。
6. **受控业务验收后恢复服务。** 使用独立测试企业、固定合成文件、受控模型/向量执行全流程和浏览器断网恢复；必须证明任务/供应商账本没有重复结算。绑定真实主机、候选和业务阶段的容量试验另按 [4C4G 方案](4c4g-business-capacity-plan.md) 执行。获准窗口、目标、请求上限和模型预算记录齐全，才开放新业务；功能通过不等于商业准入完成。

停机时长尚未实测。现有 workflow 迁移 Job 上限 2700 秒、Worker 等待 1800 秒、单次流水线 90 分钟；这些是终止上限，不能当作可承诺的用户停机时间。发布前须选择实际维护窗口并在每阶段设操作停止点。

## 按失败阶段回滚

| 失败位置 | 允许的恢复动作 | 不允许推断 |
| --- | --- | --- |
| 尚未改数据库、尚未换镜像 | 核对仍为 0027 与原模板/配置后，恢复记录的原服务副本；保留完整失败回执 | 单凭 workflow 失败就认为 DB 未变 |
| 迁移失败、取消、超时或版本未知 | 保持维护及暂停状态，读实际版本/Job；在隔离副本判断向前修复方案 | 自动 apply 候选、直接 rollout undo 或自动 downgrade |
| 已到 0031，候选尚未开放写入 | 在验证过的 0031 兼容回退镜像上恢复服务，数据库保留 0031；当前候选为 `93d95a9…`，须先完成镜像演练 | v0.1.44 可无条件读取新 JSONB 前提和新账本 |
| 候选已产生新业务/供应商历史 | 停新提交、保留在途任务、冻结和核对新历史；关闭新功能并切到已验证兼容候选或向前修复 | 恢复旧快照后仍无数据损失、回滚消费/费用为零 |
| 必须回到旧数据库快照 | 另行批准具体数据损失边界，隔离恢复 DB+对象并核对引用后切换；当前没有备用服务器 | 覆盖正式数据库或用旧对象快照伪装无损恢复 |

`rollback_release.py` 只处理 Deployment revision，不回退 ConfigMap、Secret 或数据库，也不验证业务数据读取兼容。表内 revision 是基线，不是当前即可执行的通用 rollback 指令。迁移 0028–0031 在存在后台/配额/供应商历史时会拒绝破坏性降级，应保留这些约束。

工作流收尾现在只在 prerequisites、migration、workloads 全部为 success 时恢复**已应用候选**的副本；任何失败/取消/跳过/空值均无 kubectl 调用。集群 apply/rollout 自身失败仍使该步骤失败。这个修复不会自动恢复旧数据，也不会把迁移失败转成部署成功。

## 当前交付与剩余执行项

- 已完成：线上只读基线、模板资源比较、失败恢复红→绿及边界测试、分阶段发布/回滚设计。
- 下一项：批准精确版本后制备两套候选镜像与签名清单，在本机独占恢复副本完成镜像级兼容回退。
- 后续：零供应商调用部署路线、实际可 apply 的私有配置包、当前恢复点、额度与维护窗口；业务阶段/目标绑定、浏览器恢复、当前主机容量试验。
- 独立商业门槛仍包含代表性资料/领域审核、告警实收、定期备份和整机 RPO/RTO，不能从本轮计划或 CI 推导通过。
