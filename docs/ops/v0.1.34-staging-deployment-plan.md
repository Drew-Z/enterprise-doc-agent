# v0.1.34 Staging 部署审阅

更新日期：2026-09-09，Asia/Shanghai。当前状态：第二次部署的迁移、rollout、embedding 和 readiness
通过，upload→Agent smoke 失败、治理 smoke 跳过。五个服务就绪，v0.1.34 仍未通过发布验收。

## 已完成

- 提交 `b0dde98f69f6ed9560c3c018bff2f1b4371283d5` 已推送；[Quality 34185778632](https://github.com/Drew-Z/enterprise-doc-agent/actions/runs/34185778632) 前后端通过。
- 带注释的 `v0.1.34` tag 指向该提交，tag 对象为 `3c69fe44b48a485de7ea10d8ae19e1c37943e80e`。
- [Container Supply Chain 34187312679](https://github.com/Drew-Z/enterprise-doc-agent/actions/runs/34187312679) attempt 1 全部五个作业通过。
- 四镜像的 scan、SBOM、provenance、sign、verify 全部成功。56 个原始证据文件哈希、
  签名 identity/commit/digest、SBOM 及 SLSA v1 载荷核对通过；OCI index 与 linux/amd64 平台可读取。
- Trivy 的零项结果限于现有 `CRITICAL,HIGH` 且 `ignore-unfixed=true` 的扫描范围，不能推断所有漏洞均不存在。
- 密码学验证在该次 CI 中完成，本地核对原始验证结果和来源绑定。Cosign v2.4.3 的 typed SLSA
  序列化会规范化 invocation ID 字段并省略两个 BuildKit 扩展字段；其他字段一致，原始文件哈希仍保留。
- [候选与预检证据](../../evidence/m6/20260908-v0.1.34-candidate-preflight.json)
  包含完整 workflow 输入、带旧值校验的 Namespace JSON Patch、四个回滚变量的前后值和产物来源。
- [原始发布 manifest](../../evidence/m6/20260908-v0.1.34-release-manifest.json) 按原字节保留，
  SHA-256 为 `15bf0de91defaf33e61bf35ec002db0d053353ccd7fed2db1356b9ac843a5a46`。

## 准确镜像

统一仓库前缀为 `ghcr.io/drew-z/enterprise-doc-`，部署使用下表完整 digest。

| 服务 | v0.1.34 候选 digest |
| --- | --- |
| api | `sha256:e2cbbc6ba1c5e7463ed21e6663c5a948d467e46bcd187a7cf8b02c50ed53bfff` |
| worker | `sha256:b957cdd12dff8e3151386a4a1e829b311c3970f14e22479524011f22e7d784d7` |
| consumer | `sha256:ec84d7d280ff74f28147c678428cee5cfa55a5a70a6fe444a20079e616262a21` |
| web | `sha256:618655ebcaaa3ff8af903414e348d1261b563f00becf54f06f66fad2441e3b09` |

## 首次部署前的环境与差异

2026-09-08T04:59Z 对当前 4C4G 主机的只读核查确认：Namespace UID 与受保护配置相符，
五个 Deployment 全部 1/1 Ready，应用镜像与已验收的
[v0.1.33](../../evidence/m6/20260904-v0.1.33-staging-governance.json) 一致。
主机有 4 vCPU，约 1.56 GiB 可用内存，未配置 swap。

18 个前置资源、17 个 approval annotation 与本地基线 render 一致。候选 render 的
ConfigMap、模型/embedding 路由、网络和资源配置均未变。需要的实际前置变更是：

1. 将四个 `STAGING_ROLLBACK_*_IMAGE` Environment 变量更新为当前已验收的 v0.1.33 镜像。
   现值是更早镜像；准确前后值在候选证据的 `planned_deployment.rollback_environment_variable_updates`。
2. 仅更新 Namespace 的 `approved-api-images`、`approved-worker-images`、
   `approved-consumer-images`、`approved-web-images` 和 `prerequisites-sha256` 五项。
   各服务同时允许 v0.1.34 与 v0.1.33；JSON Patch 校验 Namespace UID 及每个旧值。
3. 前置指纹由 `4e9556f8e285ef34cd1273d1533455d76f352878989a5fb65b325ef2b484bd9c`
   变为 `53ea74b2f22dc2cef444b87daf40e256f9a2eb9410ed34141857a0a01d5563d3`。
   本地模拟后 18 个资源仍通过校验；服务端 dry-run 通过，现场 resourceVersion 和 annotations 未变。

05:32Z 的实际 smoke 请求方式返回 HTTP 200，database、redis、object_store 均 up。
默认 urllib 探针曾返回 403，curl 和 Pod 内请求也均返回 200；这项差异被保留，未调整边缘规则。
同一时间通过与本地 SHA 一致的现有 reindex 代码执行 `apply=False`，得到
`selected=0, created=0, replayed=0`，模型为 `Qwen/Qwen3-Embedding-4B`，
dimension 1024、version 3；没有调用提供方或排入任务。

## 已确认的执行范围

1. 将本页末尾列出的六个文件创建为一个文档/证据提交并推送，核验对应 Quality CI。
   部署仍固定使用已经构建的 `v0.1.34`/`b0dde98`，不能用之后的文档提交代替镜像来源。
2. 复查 tag、四个 digest、runner 空闲状态、Namespace 身份、前置资源与当前镜像。
   紧接部署前再做一次只读 reindex 计划；若 selected 非零，先停止审阅，避免意外扩大重建范围。
3. 核验既有合成 smoke owner/member，刷新三份短期令牌：
   `STAGING_SMOKE_TOKEN`、`STAGING_GOVERNANCE_OWNER_TOKEN`、
   `STAGING_GOVERNANCE_MEMBER_TOKEN`。仅以内存/stdin 更新受保护 Secret，不改变成员或模型路由。
4. 按已核验的准确前后值更新四个回滚变量，并应用带旧值校验的五项 Namespace annotation 补丁。
   重新运行现有前置资源校验和服务端 workload dry-run。
5. 以 `v0.1.34` 为 ref 新启动一次 `Deploy Staging`，四个输入 digest 使用本页值，
   `registry_prefix=ghcr.io/drew-z`、`staging_base_url=https://agent.playlab.eu.cc`、
   TLS Secret 为 `enterprise-doc-staging-tls`，object-store/presign 沿用现有 R2 S3 endpoint。
   保持 `single-node-4c4g`、`run_smoke=true` 和已启用的 governance smoke。
6. 依次核验迁移、workload rollout、embedding gate、readiness、upload→Agent 与治理 smoke，
   保存精确 run/attempt、镜像和脱敏结果。应用会在迁移期间短暂停止，
   embedding gate 期间 API/Web 暂停且 consumer 临时为 4 副本；这来自既有 4C4G 流程。
7. 若任一步失败，保留失败结果和现场再诊断，不自动 rerun。回滚依据为现有 v0.1.33，
   实际回滚按故障时的状态形成具体恢复步骤。

本次部署授权需明确包含的调用与数据变化：

- embedding provider probe 为两个合成输入。
- upload→Agent smoke 上传一份合成文档并运行一个 QA Agent；治理 smoke 另外上传一份合成文档，
  执行 ACL grant/revoke、retention/legal-hold、identity binding 等既有治理检查。
  因此合计两份合成上传、一例 QA，可能发生 embedding 和模型 fallback 调用，合成产物保留。
- 当前 reindex 选中数为零。现有 embedding gate 每批上限 1000，deadline 3000 秒；
  若预检后数据状态发生变化，它仍可能选择需要重建的文档。执行时须记录实际 selected/created 数。
- 工作流上限 90 分钟，Worker rollout 最长 1800 秒，API/consumer/Web 各 600 秒。
  案例数和时限不是金额上限；费用不可观测时保持未知。
- 本清单不包含新的 12/40 例 RAG 真实评测。历史三例失败的根因仍未知，M5/M7 门槛仍开放。

用户于 2026-09-08T10:47:38Z 在会话 `01a07cf9-63aa-70b0-9e02-0aefc8433e45`
审阅本清单后回复“可以”，确认这一个六文件提交、推送、CI 核验及上述一次部署和可能计费的 smoke。
该轮在执行前因会话错误中断，随后由新会话重新核查现场并执行；实际结果见下方终态记录。
确认不包含新的 12/40 例真实 RAG 评测，也不包含自动 rerun 或回滚。
候选预检 JSON 保留确认前的原始快照，不将历史 `pending_review` 或零次现场变更改写为执行结果。
这对应 [下一阶段计划](NEXT_STAGE_PLAN.md) 已约定的“镜像可审阅后确认实际部署范围”。

## 本轮文档提交清单

交付核验已完成：6 项 evidence 契约测试、Trellis 的 implement/check 各 3 项上下文校验通过。
56 个原始供应链文件和 6 个生成文件的哈希匹配，归档 manifest 与下载原字节一致，
候选记录与原始观测及准确部署输入一致；20 个本地文档链接和全部六文件空白检查通过。
索引仅增加两个 M6 引用，历史失败报告字节未变，M5/M7 仍为 `blocked_external`。

提交消息：`docs: record v0.1.34 candidate and deployment plan`。

```text
docs/ops/NEXT_STAGE_PLAN.md
docs/ops/v0.1.34-staging-deployment-plan.md
.trellis/tasks/07-19-m5-observability-eval-load/implement.md
evidence/index.json
evidence/m6/20260908-v0.1.34-candidate-preflight.json
evidence/m6/20260908-v0.1.34-release-manifest.json
```

这一个新提交及其推送与以上部署步骤合并为一次确认，避免对同一已审阅范围反复询问。
提交执行规则仍为 Trellis Phase 3.4；不 amend、不强推。M5 任务不归档。

## 已执行与终态

- 六文件提交 `ffca3d0861ae22c857fef54260b9fc4c083e980e` 已推送；
  [Quality 34224086610](https://github.com/Drew-Z/enterprise-doc-agent/actions/runs/34224086610)
  attempt 1 前后端通过，85 项本地部署/证据定向测试通过。
- 重新核验 tag、四个 OCI index、18 个前置资源与 17 项 approval annotation；现场无配置漂移，
  五个 Deployment 为 1/1，readiness 三个依赖 up，只读 reindex 再次选中零项。
- 三份八小时令牌经真实 session/role/capabilities 校验后以内存/stdin 刷新；未修改成员。
  四个回滚变量和五项 Namespace annotation 已按本清单更新，候选前置资源及 workload 服务端 dry-run 通过。
- 首次部署 [34225700580](https://github.com/Drew-Z/enterprise-doc-agent/actions/runs/34225700580)
  attempt 1 于 `2026-09-08T12:22:21Z` 启动，ref 为 `v0.1.34`，源码仍为 `b0dde98`。
  作业于 `13:40:15Z` 以 `failure` 结束，迁移和恢复步骤失败，后续 embedding、readiness、
  upload→Agent 与治理 smoke 全部跳过。原始结论保留在
  [部署失败执行记录](../../evidence/m6/20260908-v0.1.34-staging-deployment-failure.json)。
- API 镜像拉取耗时 `2744.963` 秒，超过迁移 Job 的 `2700` 秒期限；Job 在 `13:08:30Z`
  达到 `DeadlineExceeded`，镜像于 `13:09:16Z` 才拉取完成，容器随即启动并被停止。
  迁移日志未取得，不能宣称 Alembic/checkpointer 初始化成功，也不能把缺失日志当作未执行的证明。
- 既有 `always()` 恢复步骤应用的是 v0.1.34 候选 workload，不是 v0.1.33 回滚。
  Worker 在 `13:39:42Z` 超过 1800 秒 progress deadline，恢复步骤因此失败；
  `13:44Z` 快照中仅 API/Web/Redis Ready，公开 readiness 虽为 200，并不代表异步链路可用。
- 工作流结束后，Worker 于 `14:09:09Z`、Consumer 于 `14:29:15Z` 完成拉取并启动，
  分别耗时 `3567.389` 和 `4773.747` 秒。`14:47Z` 的新快照确认五个 Deployment 均 1/1 Ready，
  四个运行镜像 digest 匹配，Worker/Consumer 内三份诊断源码均与 `b0dde98` 的 SHA-256 一致。
  readiness 三个依赖 up，18 个前置资源、17 项 approval annotation 及保护变量无漂移。
  这是后续就绪状态，不补记任何已跳过的部署 gate；v0.1.34 仍未验收。
- 原始 artifact `10058749187` 的 ZIP SHA-256 为
  `ec90f3f529eafe8bf7e091d87dd26e21a471a8336c10ed1255f8a04b5ac7ff6d`。
  发布 record、manifest 与其 18 个文件全部核验；连同本轮观测共 37 份原始文件按字节归档。
  `.gitattributes` 仅为该归档目录补充禁用换行转换，避免 YAML/TXT 在 Git 往返后破坏原始哈希。
- 保留工作流现有超时。没有 workflow rerun、镜像 relay、路由/资源扩大、回滚或新的真实 RAG trial。
  本次工作流的 embedding probe、合成上传和 QA 均为零，因为相关步骤跳过；后台调用及账户总费用
  未测量，不能据此宣称全环境零费用。历史三例 RAG 失败仍未归因，M5/M7 保持 `blocked_external`。

## 已确认并执行的再次验收

用户在会话 `01a080bd-0114-7483-93f5-21c016b0f272` 审阅本节后回复“可以”。
授权记录于 `2026-09-08T15:55:14Z` 保存，绑定本页当时的 SHA-256
`d473f4a1a7385bd833bc7a59438a53980406c9b10bd96b3e1ecb53841c20b691`。
下列范围已执行一次，结果见下一节；该授权不延续为第三次部署，也不包含本次归档的提交或推送。

1. 重新只读核对 Namespace UID、`v0.1.34` tag、四个 digest、runner 空闲状态、18 个前置资源、
   保护变量和五个 Deployment。`14:54Z` 已确认四个候选镜像均在 containerd/CRI 缓存中，
   执行前须再次确认。用既有代码做 `apply=False` reindex 计划，selected 非零则停止。
2. 检查三份既有 smoke 令牌的 session/role/capabilities 与剩余有效期。它们于 `20:18:48Z`
   或 `20:18:49Z` 到期；若不足以覆盖 90 分钟工作流及余量，按原流程仅在内存/stdin 刷新同三份
   Secret，不改成员、不落盘、不输出值。
3. 仅清理 Namespace `enterprise-doc-agent-staging` 中已归档的失败 Job `enterprise-doc-migrate`，
   准确 UID 为 `305d2d82-20ac-4bc3-abec-d552a5fac40f`。删除前复核 UID、`Failed=True`、
   active/succeeded 均零，并保留最终状态；若 UID 变化或存在活跃执行则停止。
   这是删除 Kubernetes Job 历史，不删除数据库、R2 对象、工作负载或 Namespace；原 UID 无法恢复，
   后续工作流会创建新 Job。若已由既有 24 小时 TTL 清理，仅记录缺失，不额外删除其他对象。
4. 使用原候选证据中的准确 dispatch 输入，以 `v0.1.34` 启动一个新的 `Deploy Staging` run，
   不对 `34225700580` 发起 rerun。保持 `single-node-4c4g`、`run_smoke=true`、governance smoke，
   逐项等待迁移、workload、embedding、readiness、upload→Agent 和治理 gate，保留新的 run/attempt。
5. 计费及数据范围仍为两个 embedding probe 输入、两份合成上传、一例 QA，可能触发模型 fallback；
   合成数据保留。保持原有 reindex 上限及工作流时限；它们不是金额上限，实际不可观测费用保持未知。
   不包括新的 12/40 例 RAG 评测；再次失败时停下，不自动继续重试、relay 或回滚。

不需要重做已完成的四个回滚变量或五项 Namespace annotation 更新；发现漂移时先停止审阅。
本清单不授权镜像 relay、网络/模型路由/资源/超时调整或应用代码改动。

可选恢复路径是单独批准一次 `Rollback Release`：v0.1.33 的 API/Worker/Consumer revision 均为 6，
Web 为 5，四个旧镜像在 `13:29Z` 均已缓存；只读数据库 revision 为 `20260827_0020`，
两版 Alembic 迁移源码和迁移 Job 清单无差异。仍须执行前复核 revision/digest 和兼容性，
先做全部 server-side dry-run，再回滚；不降级数据库、不清理失败 Job、不扩大权限。
数据库版本相同不是本次迁移成功的证据。

## 第二次部署终态与只读诊断

- 执行前再次确认四个镜像已缓存、18 个前置资源与 17 项 approval annotation 一致、
  五个 Deployment 就绪、reindex selected 为零。三份令牌仍有超过四小时有效期，Secret 元数据、
  active owner/member 及撤销记录通过核对；本次没有刷新令牌或再次修改变量、Namespace 注解。
- 指定失败 Job 于 `15:58:55Z` 按 UID `305d2d82-20ac-4bc3-abec-d552a5fac40f`
  和 resourceVersion `339243` 条件删除，已确认缺失并停止临时代理；没有删除其他 Kubernetes 对象、
  数据库记录或 R2 对象。
- 新部署 [34248230396](https://github.com/Drew-Z/enterprise-doc-agent/actions/runs/34248230396)
  attempt 1 于 `15:59:04Z` 创建，固定原 `v0.1.34`/`b0dde98` 和同一组 digest。
  作业于 `16:03:36Z` 以 `failure` 结束：迁移、workload、rollout、embedding gate、readiness 通过，
  upload→Agent 失败，治理 smoke 跳过；既有 workload 恢复步骤成功。这不是对首次 run 的 rerun。
- 本次 embedding probe 的两个输入通过；reindex initial/final selected、created/replayed 及
  apply attempts 均为零。实际完成一份合成上传并创建一例 QA；治理步骤未执行，所以第二份合成
  上传及治理变更均未发生。QA provider 用量及总金额仍为 null，不能按零调用或零费用报告。
- 按既有合成租户、精确创建窗口、幂等键前缀和完整合成问题唯一匹配该 QA。文档为 Ready，
  Agent 在 `16:03:12.983970Z` 开始、`16:03:16.897930Z` 失败；只有一次 initial attempt，
  Job 为 dead、attempt 为 permanent_failed，错误为 `AgentExecutionRuntimeError` /
  `agent_execution_failed`，diagnostic_code 仍为空。Agent evidence 与所有 namespace 的 checkpoint
  均为零；另行确认当前 checkpointer 使用同一个应用数据库，没有独立 checkpoint URL。
- `staging_smoke.py` 在失败状态抛错后退出，尚未走到成功报告写入，因此本次没有 smoke JSON；
  缺失不能解释成未上传或未运行 QA。原始工作流日志、持久化诊断和失败发布 record 分别保留。
- `16:53Z` 读取当前 Worker/Consumer 的实际导入模块，三份源码哈希匹配发布版本；新 Python 进程
  确认分类器、allowlist 和 `AgentExecutionRuntimeError` 传递诊断码均存在。这只是新进程的检查，
  没有执行失败 Agent，也不证明当时任务由哪个进程处理。两容器均零重启；`16:02:30Z–16:04:00Z`
  内分别取得 12/3 条日志，没有对应关联 ID 或结构化错误。
- `16:54Z` 节点只发现当前两个 Kubernetes 工作进程及其两份保留日志目录；其他已删除 consumer
  的 CRI 容器记录不在本次 inventory 中。持久化 worker_id 与配置都为共享默认值 `worker-local`，
  无法单独定位实际执行进程。没有足够证据认定另一个执行端、数据库、模型或网络为根因。
- `17:27Z` 新快照确认五个 Deployment 为 1/1 Ready，四个运行 digest、诊断源码、保护变量和
  前置资源均匹配，公开 readiness 的三个依赖 up；迁移和 embedding Job 都为 Complete。
  发布仍未验收。前一会话在 QA 排查期间遇到 429/签名错误；新会话只续做诊断和归档，
  没有新的部署或模型调用。

[第二次失败执行记录](../../evidence/m6/20260909-v0.1.34-staging-redeployment-failure.json)
索引 40 份原始文件。artifact `10065067237` 的 21 个文件与原 ZIP 逐字节一致，19 项内部 manifest
哈希通过；ZIP SHA-256 为 `a67aa756bbb0fae1581a127fcb7351ed771cd4be01f816ab8e434fa7c62f3c20`。
首次失败、v0.1.33 验收及历史 RAG trial 均保留原字节。M5/M7 仍为 `blocked_external`。

下一步优先按[诊断交接](../../.trellis/tasks/07-19-m5-observability-eval-load/research/20260909-v0.1.34-smoke-diagnosis.md)
补齐执行进程归属及失败报告的可观测性。任何应用修复先形成离线复现和回归，再审阅发布范围；
当前不以第三次部署或新 12/40 例 RAG trial 代替诊断，也没有执行可选的 v0.1.33 回滚。

## 本轮归档提交边界

两次失败的归档尚未提交或推送，不属于已经使用的六文件提交或第二次部署授权。
拟合并为一个可审阅提交：`docs: record v0.1.34 deployment failures and diagnostics`，准确范围为：

```text
.gitattributes
.trellis/tasks/07-19-m5-observability-eval-load/implement.md
.trellis/tasks/07-19-m5-observability-eval-load/research/20260909-v0.1.34-smoke-diagnosis.md
docs/ops/NEXT_STAGE_PLAN.md
docs/ops/v0.1.34-staging-deployment-plan.md
evidence/index.json
evidence/m6/20260908-v0.1.34-staging-deployment-failure.json
evidence/m6/20260908-v0.1.34-staging-release/（37 份原始文件，准确清单见首次失败记录）
evidence/m6/20260909-v0.1.34-staging-redeployment-failure.json
evidence/m6/20260909-v0.1.34-staging-redeployment/（40 份原始文件，准确清单见第二次失败记录）
evidence/m6/20260909-v0.1.34-redeployment-archive-validation.json
```

提交后如一并批准推送，先回读 `origin/main`，确认仍可从 `ffca3d0` 正常 fast-forward，再推送并
核验该新 SHA 的 Quality CI；远端变化时先审查，不 amend、不强推。这不授权 tag、构建、部署、
回滚、数据变更或新的 provider 调用。历史候选、v0.1.33 及 RAG trial 原字节不变，M5 任务不归档。

首次归档的核验记录：85 项部署/证据定向测试、Trellis implement/check 各 3 项上下文、37 份原始文件
哈希、38 份文件在 `core.autocrlf=true` 下的临时 Git 往返、23 个本地链接及 `git diff --check`。
临时 index/checkout 已自动清理，未改变真实暂存区；索引仅新增一个 M6 引用，M5/M7 项完整保持原值。

新会话归档核验通过：60 项 smoke/发布记录/证据定向测试，Trellis implement/check 各 3 项上下文，
405 份既有证据原字节，两批共 77 份原始文件及两个执行记录的 79 项 Git 字节往返，以及 31 个本地
链接和 diff 检查。完整结果保存于 `evidence/m6/20260909-v0.1.34-redeployment-archive-validation.json`。
本轮索引只再增加一个 M6 引用；实际暂存区为空，M5/M7 项及旧记录未变。临时测试和 Git 目录已移除。

## 临时材料保留

- 本轮临时根目录为 `C:\Users\zhang\AppData\Local\Temp\eda-v0.1.34-deploy-01a080bd`。
  原始 ZIP、解压文件、前后现场和恢复预检仍保留供审阅；`deployment_support.py` 与
  `collect_postflight.py` 保留用于后续只读复核。
- 拟清理该根目录内的九个已完成一次性脚本、三个明确由本轮生成的 `.pyc`，以及空结果探针
  `workflow-failure-diagnostics.json`，共 13 个文件。删除命令在启动前被环境策略拒绝，
  实际均未删除；没有改用其他方式绕过限制。有效日志提取记录是已归档的 `workflow-failure-lines.json`。
- 先前任务的 `C:\Users\zhang\AppData\Local\Temp\eda-v0.1.34-review-5__gd4f6`
  及所有既有 worktree 均保留。它们不是本轮可随意清理的一次性材料。
- 新会话在逐项核验路径和 SHA-256 后，已删除自身创建的八个无后续用途文件：
  `resume_20260909_archive.py`、`resume_20260909_validate.py`、`resume-20260909-baseline.json`、
  `resume-20260909-archive.json`、`resume-20260909-runtime-attribution.json`、
  `resume-20260909-node-inventory.json`、`resume-20260909-checkpoint-target.json`、
  `resume-20260909-postflight.json`。它们均位于上述临时根目录；四份观测已有逐字节核验的仓库副本，
  两份完成脚本及重复元数据可由任务记录恢复。没有触及前一会话的文件或重试被拒绝的清理命令。
- 同一目录内保留 `resume_20260909_diagnosis.py` 用于继续调查，以及
  `resume-20260909-postflight-live.json`、`resume-20260909-postflight-namespace.json`、
  `resume-20260909-postflight-prerequisites.json` 三份完整现场快照。
- 工作区审计中的 `D:\workspace4Cursor\enterprise-doc-agent-actions-minutes` 为未合并 worktree，
  `D:\workspace4Cursor\enterprise-doc-agent-admin-v014` 有既有未提交内容；
  `D:\workspace4Cursor\enterprise-doc-agent\tmp\admin-prereq-v0.1.8-29972941762` 的登记指向不存在位置。
  本轮均未清理；任何后续回收仍须重新核对归属、任务使用状态和批准范围。
