# 产业化审计与正式交付计划

审计日期：2026-09-24。代码基线：`ad1863dc099326d12d3fc18ed67eb7e072184fa4`。

## 结论

当前是**已上线的受控试点，有完整演示业务链，尚未达到正式商业服务条件**。主要差距集中在真实模型可靠性、所有付费调用的计量、当前主机的运维验收和经营闭环。继续增加登录方式或另建 Keycloak 不能解决这些问题。

2026-09-24 用户已确定首版为**受邀企业正式版**：企业准入、稳定业务流程、人工复核与运营管理。公开自助 SaaS 保留为后续阶段，支付接入不阻塞首版。业务主机维持现有 4C4G，用户明确不配置备用服务器。

已确定计量口径：通用 Agent 按成功业务任务计量，文档解析使用独立处理配额；供应商实际调用及未知成本单独记录。后续公开经营拟采用个人经营、人民币，支付商、套餐及退款规则尚未确定。告警接收地址已在本地运维任务中登记；演练结果由用户核对，不伪造独立审核。

本次修改尚未上线；PR #8 仍有 v7 模型语义门槛，不能因本轮代码测试通过直接合并发布。此前上线记录是 v0.1.44 / presales.v3。最新只读探测可见 ready 与匿名 GitHub 会话接口；它们不暴露实际镜像版本，也不证明登录或完整业务成功。

## 有证据的缺口矩阵

| 优先级 / 范围 | 已有能力与证据 | 尚缺与影响 | 关闭条件 / 责任 |
|---|---|---|---|
| P0 模型语义 | v0.1.44 六题：6 个草稿、5/6 分类；v7 新渠道六次调用仅3有效草稿，存在 unknown→unmet 错误。[质量记录](presales-quality-evaluation.md) | 提示词/渠道升级未获得稳定语义验收；不能对客户自动作承诺 | 冻结企业代表资料与独立 gold，保留全部调用/失败，验证未知、否定、条件、冲突及数字；产品与独立领域审核人批准 |
| P0 上游波动体验 | 分支持久任务、租约、最多两次调用、冷却、部分成功、刷新恢复已本地验证；未部署，默认关闭。[手册](public-pilot-runbook.md#后台生成与有界渠道恢复) | 线上用户仍受长请求及渠道波动影响；两个渠道同时失败仍可能无结果 | 当前候选公网验证断网/刷新/重复提交/主路失败/两路失败；资料和成功结果不丢，明确可重试，有界等待，准确记录未知成本 |
| P0 准入/额度窗口 | `billing/service.py` 原无历史周期走 legacy；正式运营分两步配置。[运营入口](platform-operations.md) | 新企业可能在未配置周期时生成 | 本轮已实现正式环境有限周期约束；真实 DB/同步及后台入口回归通过。发布前为现有企业补齐有效有限周期，排空旧在途任务 |
| P0 全产品计量 | 本分支已实现 Agent 成功任务和独立文档字节配额；逐次 Agent/向量调用记录、有限次数预算及用量页已本地验证。[迁移与配置](platform-operations.md#给既有周期补充产品额度) | 尚未部署、未补齐现有企业配额；无版本化价目表、真实供应商金额对账；用量页不是账单 | 排空旧任务后迁移并配置，主/备/超时/取消样本与供应商真实记录核对；未知费用保留，完成当前候选公网业务验收 |
| P0 当前业务容量 | 旧4C8G的3600次readiness试验；公网 p95=1825ms，高于原250ms目标。[证据](../../evidence/m5/20260817-application-capacity.json) | 不能推导当前4C4G可承载多少企业、上传或生成；单节点不具备高可用 | 4C4G真实上传/解析/检索/队列/恢复组合负载至少两轮，记录CPU/RSS/DB池/队列及允许的并发；以实际结果设置准入和限流，运营批准容量上限 |
| P0 灾备 | 2026-08-06 staging→本地 Docker，RPO153.6s/RTO166.4s达到当次目标；DB、对象和引用核验已有实现。[演练](../../evidence/m6/20260806-production-rpo-rto-drill-passed.json) | 旧版本、非独立故障域、未独立签字；不能承诺当前节点损坏后恢复 | 按用户决定不配置备用服务器。准备当前候选 DB+对象的隔离恢复、原文/产物哈希和回滚核验；异机备份及整机故障恢复仍需证据。现有独立故障域和 RPO≤300s/RTO≤1800s 门槛保持未通过，不能用同机测试替代或自动放宽 |
| P0 告警/值班 | Prometheus规则和采集存在；现有门槛明确未配置告警投递。[门槛](../../evidence/gates/m5-managed-observability.json) | 无可靠故障通知、接收责任与备份新鲜度证明 | 外部探测+队列/磁盘/备份监控，实发测试故障及恢复通知并取得回执，明确值班人和处理时限。避免给4C4G增加重量级常驻服务 |
| P0 发布治理 | 已有Quality、镜像扫描/签名和手动部署流程；本轮GitHub只读：main未保护、ruleset=0、staging仅branch policy | 代码或配置可能绕过必要评审；草稿候选不能当已发布 | 配置强推/删除保护、准确CI检查及独立发布审批；先处理Quality对纯文档提交的路径跳过，避免必需检查永远pending。实际规则读回、负例和回滚验收 |
| P0 数据与合同 | 租户隔离、权限、审计归档、legal hold、导出已有实现 | 审计归档不删除源事件；条款、供应商/地域披露、客户退出和删除证明尚无完整经营验收 | 经营方确定保留/删除与备份周期，完成客户数据导出、删除工单和证明；隐私/服务条款经合适人员审核，不自行宣称合规认证 |
| P1 企业运营 | GitHub浏览器登录、企业准入、邀请、owner/member、私有CLI和用量页已有实现 | 运营仍依赖命令行和人工协调；支持渠道、客服排障与试点验收未闭环 | 把开户、额度、到期、成员退出、工单和费用对账写成可演练流程，至少完成受邀客户UAT；先用现有CLI，需求成立再开发运营后台 |
| P1 自助收费 | 有有限周期和幂等运营配置，没有支付/订单/退款集成 | 无法安全开放自助付费与自动续期 | 经营主体、币种/价格/退款规则及支付商户明确后，完成签名回调、重放/乱序、续期/取消/退款/对账的sandbox和真实回执 |
| P1 客户验证 | 公开演示与合成资料试验已完成多轮 | 不代表目标客户能节省时间或愿意付费 | 客户授权资料、业务审核、基准耗时、问题清单和验收签字，记录合同与单客户毛利测算，不以演示流量替代 |
| P2 可维护性 | 代码/CI/规范覆盖较多；根README区分候选和部署 | 旧展示文档认证结论过时；48个历史任务状态与后续事实有漂移，基线2964项历史改动 | 本轮新增单一产品化入口并标记历史文档范围；后续按所有权整理任务和精确提交，不清理归属不明文件，不以测试数量替代发布证据 |
| P2 分发 | 仓库公开可读，但README明确尚无LICENSE | 开源/商业分发授权边界未定 | 经营方选择分发许可；不擅自添加MIT/Apache或据此向客户作许可承诺 |

最新只读数据见 [2026-09-24审计记录](../../evidence/gates/commercial-audit-20260924.json)。默认 Python HTTP 客户端曾返回 HTTP 错误，使用明确浏览器 User-Agent 的后续探测得到200；两次观察都保留，尚不足以把差异归因为应用宕机或特定WAF规则。

## 执行任务与依赖

总任务：`.trellis/tasks/09-24-commercial-production-readiness/`，隶属原平台总任务，不另建仓库。

| 阶段 | 子任务 | 交付及完成条件 | 当前状态 |
|---|---|---|---|
| 1 可运营底座 | `commercial-entitlement-enforcement` | 正式进程有限周期、新预约拒绝、旧账本兼容、运营切换流程 | 本轮代码及有界集成通过，未部署 |
| 1 发布证据 | `commercial-release-gate` | CLI及手动CI拒绝缺失/篡改/过期/错版本/错环境证据，复用恢复/容量验证器 | 本轮实现；实际候选仍blocked |
| 2 全产品成本 | `commercial-model-metering` | Agent成功任务额度、独立文档处理配额和供应商实际调用账本 | CM-1～4已本地验证；CM-5已补供应商标识与私有只读对账导出，真实账单核对、定价和部署验收仍待完成 |
| 2 当前环境验收 | `commercial-operations-acceptance` | 质量→业务→容量→告警→独立恢复→发布治理→受邀客户UAT | 真实质量与外部资源/责任仍未满足 |
| 后续公开收费 | `commercial-self-service` | 定价/条款/商户→订单/回调→续费退款→自助准入→退出→sandbox/生产回执 | 拟个人经营、人民币；支付商等未定，不阻塞受邀首版 |
| 4 正式交付 | 总任务整合 | 精确版本、证据、运维手册、恢复点、责任人和客户签收；控制规模上线并观察 | 未达到 |

每阶段先固定可观测行为，再红→绿实现和回归；外部失败保留，不自动重试来“刷绿”。阶段1不能代替阶段2，阶段2不能代替公开SaaS阶段3。上线日期由门槛决定，现阶段没有依据承诺固定商用日。

## 发布检查

```powershell
& .\.venv\Scripts\python.exe -B scripts/check_commercial_readiness.py `
  --manifest evidence/gates/commercial-readiness-current.json `
  --commit ad1863dc099326d12d3fc18ed67eb7e072184fa4 `
  --environment staging --profile single-node-4c4g --scope invited
```

当前返回exit 1 / `blocked`是准确结果。公开自助目标使用`--scope public_saas`且必须增加payments和self_service门槛。候选源SHA发生变化后需更新证据，不能直接改manifest SHA冒充重测。

固定门槛及每项检查定义在`check_commercial_readiness.py::REQUIRED_CHECKS`。每项通过记录须包含：

- `schema_version=1`、准确`gate_id/status=passed`、`commit_sha/environment/profile`、`completed_at`和`executed_by`。
- `review.status=approved`、不同执行者的`reviewed_by`、晚于执行且不在未来的`reviewed_at`。
- 所有固定`checks`为true，以及至少一份`artifacts[{path,sha256}]`绑定的JSON原始执行报告；原始报告必须passed、同SHA/环境/profile、时间不晚于总结。
- capacity/recovery另提供`measurement_report`，满足既有`validate_recovery_capacity_evidence.py`，且目标一致；capacity必须实际覆盖upload、ingestion、retrieval、generation_recovery，不能用ready代替。
- capacity还需执行前批准的`acceptance_objectives`（max_p95_ms、max_error_rate、min_headroom_percent、approved_by、approved_at），实测不达目标时即使报告标passed也会拒绝。
- 执行报告距检查时间不超过7天；这是新发布复核的证据新鲜度规则，不是长期存储保留期。未执行项目使用blocked/needs_implementation，不能创建伪passed模板。

检查成功只表示`eligible_for_release_review`；哈希不是签名，审核者字段也不是身份认证。真实性、代表性、模型语义和经营接受需要实际审核，最终部署仍由现有发布授权流程决定。手动`Commercial Readiness`工作流不部署、不调用模型、不发送通知，也不会自动为现有staging流水线增添已满足的生产门槛。

## 正式验收所需输入

1. 产品/经营：首版受邀企业、Agent成功任务与独立文档处理配额口径已确认；仍需首批客户、价格/合同和数据政策。公开付费的支付商、套餐与退款规则另行确定。
2. 运维：告警邮箱已指定，不配置备用服务器；[单节点执行手册](single-node-operations-acceptance.md)已补齐。0030 合成账本的本地真实数据库恢复通过；通知服务接入、真实 DB＋对象恢复、备份存储/保留规则和整机故障恢复证据仍待完成，演练结果由用户核对。
3. 质量：客户授权的代表资料与独立评判、允许的真实模型验证批次/成本上限。现有v7六次失败记录和停止的18题不会被重新计为通过。

这些输入决定外部验收和商业承诺。代码、文档、受控测试继续推进；不会自行发送通知、部署备用服务器、选择商户或制造客户批准。
